[pytest]
# Tell pytest-django which settings module to boot Django with (same path Django itself uses).
DJANGO_SETTINGS_MODULE = config.settings
# Which filenames pytest should treat as test modules.
python_files = tests.py test_*.py *_tests.py
